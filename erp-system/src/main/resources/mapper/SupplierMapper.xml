<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.erpsystem.mapper.SupplierMapper">
    <resultMap id="BaseResultMap" type="com.example.erpsystem.entity.Supplier">
        <id column="id" property="id"/>
        <result column="supplier_name" property="supplierName"/>
        <result column="contact_person" property="contactPerson"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="address" property="address"/>
        <result column="bank_name" property="bankName"/>
        <result column="bank_account" property="bankAccount"/>
        <result column="tax_number" property="taxNumber"/>
        <result column="initial_payable" property="initialPayable"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="getList" resultMap="BaseResultMap">
        SELECT * FROM supplier
        <where>
            <if test="keyword != null and keyword != ''">
                AND (supplier_name LIKE CONCAT('%', #{keyword}, '%') OR contact_person LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            AND status = 1
        </where>
        ORDER BY created_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO supplier (supplier_name, contact_person, phone, email, address, bank_name, bank_account, tax_number, initial_payable, status, remark)
        VALUES (#{supplierName}, #{contactPerson}, #{phone}, #{email}, #{address}, #{bankName}, #{bankAccount}, #{taxNumber}, #{initialPayable}, #{status}, #{remark})
    </insert>

    <update id="update">
        UPDATE supplier
        <set>
            <if test="supplierName != null">supplier_name = #{supplierName},</if>
            <if test="contactPerson != null">contact_person = #{contactPerson},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="address != null">address = #{address},</if>
            <if test="bankName != null">bank_name = #{bankName},</if>
            <if test="bankAccount != null">bank_account = #{bankAccount},</if>
            <if test="taxNumber != null">tax_number = #{taxNumber},</if>
            <if test="initialPayable != null">initial_payable = #{initialPayable},</if>
            <if test="remark != null">remark = #{remark},</if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM supplier WHERE id = #{id}
    </select>

    <update id="deleteById">
        UPDATE supplier SET status = 0, updated_at = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>
</mapper>