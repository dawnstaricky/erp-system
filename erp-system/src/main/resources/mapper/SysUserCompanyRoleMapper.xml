<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.erpsystem.mapper.SysUserCompanyRoleMapper">
    <resultMap id="BaseResultMap" type="com.example.erpsystem.entity.SysUserCompanyRole">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="company_id" property="companyId"/>
        <result column="role_id" property="roleId"/>
        <result column="role_code" property="roleCode"/>
        <result column="company_name" property="companyName"/>
    </resultMap>
    <select id="selectCompaniesByUserId" resultType="com.example.erpsystem.dto.UserCompanyRoleDTO">
        SELECT ucr.company_id AS companyId, c.company_name AS companyName,
               ucr.role_id AS roleId, r.role_code AS roleCode
        FROM sys_user_company_role ucr
        JOIN sys_company c ON c.id = ucr.company_id
        JOIN sys_role r ON r.id = ucr.role_id
        WHERE ucr.user_id = #{userId} AND c.status = '1' AND r.status = 1
        ORDER BY ucr.company_id, r.id
    </select>
    <select id="selectCompanyIdsByUserId" resultType="java.lang.Long">
        SELECT DISTINCT company_id FROM sys_user_company_role WHERE user_id = #{userId}
    </select>
    <select id="selectByUserCompany" resultMap="BaseResultMap">
        SELECT * FROM sys_user_company_role WHERE user_id=#{userId} AND company_id=#{companyId}
    </select>
    <insert id="insert">
        INSERT INTO sys_user_company_role(user_id,company_id,role_id) VALUES(#{userId},#{companyId},#{roleId})
    </insert>
    <insert id="insertBatch">
        INSERT INTO sys_user_company_role(user_id,company_id,role_id) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId},#{item.companyId},#{item.roleId})
        </foreach>
    </insert>
    <delete id="deleteByUserId">DELETE FROM sys_user_company_role WHERE user_id=#{userId}</delete>
    <delete id="deleteByUserCompany">DELETE FROM sys_user_company_role WHERE user_id=#{userId} AND company_id=#{companyId}</delete>
</mapper>