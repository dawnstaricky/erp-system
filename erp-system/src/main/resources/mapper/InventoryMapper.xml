<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.erpsystem.mapper.InventoryMapper">

    <resultMap id="BaseResultMap" type="com.example.erpsystem.entity.InventoryVO">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="warehouse_id" property="warehouseId"/>
        <result column="quantity" property="quantity"/>
        <result column="lock_quantity" property="lockQuantity"/>
        <result column="cost_price" property="costPrice"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="product_name" property="productName"/>
        <result column="sku_code" property="skuCode"/>
        <result column="spec" property="spec"/>
        <result column="unit" property="unit"/>
        <result column="min_stock" property="minStock"/>
    </resultMap>

    <!-- 根据商品和仓库查询库存 -->
    <select id="selectByProductAndWarehouse" resultMap="BaseResultMap">
        SELECT i.*, p.product_name, p.sku_code, p.spec, p.unit, p.min_stock
        FROM inventory i
                 LEFT JOIN product p ON i.product_id = p.id
        WHERE i.product_id = #{productId} AND i.warehouse_id = #{warehouseId}
    </select>

    <!-- 新增库存记录 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO inventory (product_id, warehouse_id, quantity, lock_quantity, cost_price)
        VALUES (#{productId}, #{warehouseId}, #{quantity}, #{lockQuantity}, #{costPrice})
    </insert>

    <!-- 库存列表 -->
    <select id="getList" resultMap="BaseResultMap">
        SELECT i.*, p.product_name, p.sku_code, p.spec, p.unit
        FROM inventory i
        LEFT JOIN product p ON i.product_id = p.id
        <where>
            <if test="warehouseId != null">
                AND i.warehouse_id = #{warehouseId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (p.product_name LIKE CONCAT('%', #{keyword}, '%')
                OR p.sku_code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            AND p.status = 1
        </where>
        ORDER BY i.quantity ASC
    </select>

    <!-- 库存预警 -->
    <select id="getLowStockList" resultMap="BaseResultMap">
        SELECT i.*, p.product_name, p.sku_code, p.min_stock
        FROM inventory i
                 LEFT JOIN product p ON i.product_id = p.id
        WHERE i.warehouse_id = #{warehouseId}
          AND p.min_stock IS NOT NULL
          AND i.quantity &lt; p.min_stock
        ORDER BY i.quantity ASC
    </select>

</mapper>