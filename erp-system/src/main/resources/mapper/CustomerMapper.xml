<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.erpsystem.mapper.CustomerMapper">

    <resultMap id="BaseResultMap" type="com.example.erpsystem.entity.Customer">
        <id column="id" property="id"/>
        <result column="customer_name" property="customerName"/>
        <result column="contact_person" property="contactPerson"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="address" property="address"/>
        <result column="level" property="level"/>
        <result column="credit_limit" property="creditLimit"/>
        <result column="initial_receivable" property="initialReceivable"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="getList" resultMap="BaseResultMap">
        SELECT * FROM customer
        <where>
            <if test="keyword != null and keyword != ''">
                AND (customer_name LIKE CONCAT('%', #{keyword}, '%') OR contact_person LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            AND status = 1
        </where>
        ORDER BY created_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO customer (customer_name, contact_person, phone, email, address, level, credit_limit, initial_receivable, status, remark)
        VALUES (#{customerName}, #{contactPerson}, #{phone}, #{email}, #{address}, #{level}, #{creditLimit}, #{initialReceivable}, #{status}, #{remark})
    </insert>

    <update id="update">
        UPDATE customer
        <set>
            <if test="customerName != null">customer_name = #{customerName},</if>
            <if test="contactPerson != null">contact_person = #{contactPerson},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="address != null">address = #{address},</if>
            <if test="level != null">level = #{level},</if>
            <if test="creditLimit != null">credit_limit = #{creditLimit},</if>
            <if test="remark != null">remark = #{remark},</if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM customer WHERE id = #{id}
    </select>

    <update id="deleteById">
        UPDATE customer SET status = 0, updated_at = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>

</mapper>